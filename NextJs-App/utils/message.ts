//import ModalMessage from "@/components/Modal";
import ModalMessage from "@/components/ModalMessage";
import { title } from "process";
//import ModalMessage from "../components/ModalMessage";
import ReactDOM from "react-dom";


export function showMessage(modalTitle: string, modalMessage: string): void {
    /*
    const overlayElement = document.createElement('div');
    overlayElement.id = 'overlay-root';
    document.body.appendChild(overlayElement);
    overlayElement.appendChild();
    ReactDOM.render(
        <Message open={true} title={modalTitle} message={modalMessage} />,
      //<Loader isVisible={isVisible} label={label} />,
      overlayElement
    );
    */
}